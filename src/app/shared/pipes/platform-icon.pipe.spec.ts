import { PlatformIconPipe } from './platform-icon.pipe';

describe('PlatformIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformIconPipe();
    expect(pipe).toBeTruthy();
  });
});
